<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.demo.budjetkeeperver2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BudjetKeeperVer2"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".TestChartActivity"></activity>
        <activity android:name=".SettingsActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".categorydb.AddCategoryActivity" />
        <activity android:name=".billsdb.AddBillActivity" />
        <activity android:name=".notesdb.AddNoteActivity" />
        <activity android:name=".MenuBillsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>